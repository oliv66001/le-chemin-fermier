{% block body %}
	<div class="container-fluid">
		<div class="row">
			<div class="col-md-12">

				<ul>

					{% for categorie in categories %}

						{% if categorie.slug == "Boissons" %}
							<img src={{ categorie.imageUrl }} alt="{{ categorie.name }}"/>
						{% endif %}
					{% endfor %}
					<h1 class="text-center mt-10">Nos boissons et vins :
					</h1>
					{% for boisson in boissons %}

						<section class="container">
							<div class="row ">
								<div class="col">
									<ol>
										<h5>Nom :</br>
									</h5>
									<h3 class="text-center mt-10">
										{{ boisson.name }}</h3>
								</ol>
								<ol>
									<h5>Description :</br>
								</h5>
								<h5>{{ boisson.description }}</h5>
							</ol>
							<ol>
								<h5>Prix :</br>
							</h5>
							<h3 class="text-center">
								{{ boisson.price }}€</h3>
						</ol>
					</div>
				</div>
			</section>

		{% endfor %}
	</ul>
</div></div><div>{% endblock %}
