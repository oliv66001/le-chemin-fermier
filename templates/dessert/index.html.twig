{% block body %}
	<div class="container-fluid">
		<div class="row">
			<div class="col-md-12">

				<ul>

					{% for categorie in categories %}

						{% if categorie.slug == "Desserts" %}
							<img src={{ categorie.imageUrl }} alt="{{ categorie.name }}"/>
						{% endif %}
					{% endfor %}
					<h1 class="text-center mt-10">Nos desserts :
					</h1>
					{% for dessert in desserts %}

						<section class="container-fluid">
							<div class="row justify-content-center">
								<div class="col-md-6">
									<ol>
										<h5>Nom :</br>
									</h5>
									<h3 class="text-center mt-10">
										{{ dessert.name }}</h3>
								</ol>
								<ol>
									<h5>Description :</br>
								</h5>
								<h5>{{ dessert.description }}</h5>
							</ol>
							<ol>
								<h5>Prix :</br>
							</h5>
							<h3 class="text-center">
								{{ dessert.price }}€</h3>
						</ol>
					</div>
				</div>
			</section>

		{% endfor %}
	</ul>
</div></div><div>{% endblock %}
